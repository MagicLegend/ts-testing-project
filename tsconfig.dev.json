{
  "extends": "./tsconfig.base.json",
  "compilerOptions": {
    "allowJs": true,
    "outDir": "bin",
    "sourceMap": true,
    "noUnusedLocals": false, // Overrule base config because I'm lazy and I don't want it to yell at me every time 
    // "baseUrl": "./"
  },
  "include": [
    "src"
  ],
  "exclude": [
    "node_modules",
    "bin",
  ]
}
